<%= form_with(model: hierarchy, local: true) do |form| %>
  <% if hierarchy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hierarchy.errors.count, "error") %> prohibited this hierarchy from being saved:</h2>

      <ul>
      <% hierarchy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :hierarchy_name %>
  </div>

  <div class="field">
    <%= form.label :acronym %>
    <%= form.text_field :acronym, id: :hierarchy_acronym %>
  </div>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :hierarchy_rank %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
