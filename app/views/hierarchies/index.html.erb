<h3><%= I18n.t 'activerecord.hierarchies'%></h3>
<p id="notice"><%= notice %></p>
<div class="box">  
  <!-- /.box-header -->
  <div class="box-body">
    <%= search_form_for @q, html: {class: "form-horizontal", role: "search"} do |f| %>
      <%= f.search_field :name_or_acronym_or_rank_cont, class: "form-control", placeholder: t('helpers.layouts.search') %>
    <% end %>
    <table class="table table-responsive table-hover">
      <thead>
        <tr>
          <th><%= sort_link(@q, :name, t('activerecord.default.name')) %></th>
          <th class="col-sm-1 text-center"><%= sort_link(@q, :acronym, t('activerecord.attributes.acronym')) %></th>
          <th class="col-sm-1 text-center"><%= sort_link(@q, :rank, t('activerecord.attributes.rank')) %></th>
          <th class="col-sm-2 text-center"><%= I18n.t 'helpers.layouts.action' %></th>
        </tr>
      </thead>
      <tbody>
        <% @hierarchies.each do |hierarchy| %>
          <tr>
            <td><%= hierarchy.name %></td>
            <td align="center"><%= hierarchy.acronym %></td>
            <td align="center"><%= hierarchy.rank %></td>
            <td align="center">
              <%= hidden_field(@hierarchy, hierarchy) %>
              <%= link_to t('helpers.layouts.edit'), edit_hierarchy_path(hierarchy.id), class: 'btn btn-default btn-xs' %>
              <%= link_to t('helpers.layouts.destroy'), hierarchy, method: :delete, data: { confirm: t('helpers.submit.sure') }, class: 'btn btn-default btn-xs' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>